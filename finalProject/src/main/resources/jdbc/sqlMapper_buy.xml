<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="test.com.idle.vo">
	<select id="BUY_SELECT_ALL" resultType="test.com.idle.vo.BuyVO">
		select * from (select rownum rnum, buy_num, board_num, id
		from (select buy_num, board_num, id
		from buy where id = 'tester3' order by buy_num desc))
		where rnum &gt;= #{startRow} and rnum &lt;= #{endRow}
	</select>
	
	<select id="BUY_ROW_COUNT" resultType="Integer">
		select count(*) cnt from buy
	</select>
	
	<delete id="BUY_DELETE">
		delete from buy where buy_num = #{buy_num}
	</delete>
	
</mapper>